<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sethu.androidtemplatecode">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".application.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.RetrofitDemoActivity" />
        <activity android:name=".lrucachedemo.LRUMainActivity" />
        <activity android:name=".lrucachedemo.ViewImageActivity" />
        <activity android:name=".contentproviderdemo.MainActivity" />
        <activity android:name=".coordinatorlayoutdemo.FabAndSnackbarActivity" />
        <activity android:name=".coordinatorlayoutdemo.FabFollowsWidgetActivity" />
        <activity android:name=".coordinatorlayoutdemo.MainActivity"

            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".coordinatorlayoutdemo.CollapsingToolbarActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <provider
            android:name=".contentproviderdemo.data.FlavorsProvider"
            android:authorities="@string/content_authority"
            android:exported="false"
            android:syncable="true" />

        <activity android:name=".coordinatorlayoutdemo.CoordinatorLayoutExamples"></activity>
    </application>

</manifest>